name: Brand guard

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  brand:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: ./scripts/test-check-brand.sh
      - run: ./scripts/check-brand.sh

